program Registros;
uses crt;
type t_registro=record

     end;

var registro:t_registro;


    procedure inicializar(var r:t_registro);
    begin
         with r do
         begin

         end;
    end;

    procedure cargar(var r:t_registro);
    begin
         with r do
         begin

         end;
    end;

    procedure mostrar(var r:t_registro);
    begin
         with r do
         begin

         end;
    end;


begin
     inicializar(registro);
     cargar(registro);
     mostrar(registro);


readkey;
end.
